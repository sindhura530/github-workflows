name: Inputs
on:
  workflow_call:
  workflow_dispatch:
  
jobs:
  
  test:
    runs-on: ubuntu-latest
    steps:
       - name: Input values
         run: |
           declare -A array
           array=( [pipelineType] = "azstaticwebapp" 
                   [language] = "node" 
                   [nodeVersion] ="16"
                   [buildTool] = "npm"
                   [BUILD_image_name] = "node:16"
                   [Deployment_image_name] = "node:16"
                   [appName] = "abcccddd" )
                   
          
