name: pipelineConfig
on:
  workflow_call:
      outputs:
         pipelineType:
            value: 'azureappservice'
         language:
            value: 'node'
         nodeVersion:
            value: 12
         angularVersion:
            value: 12
         buildTool: 
            value: 'npm'
         appName: 
            value: abc
         NewJarFileName: 
            value: 'app.jar'
         TragetFileType: 
            value: 'jar'
jobs:
  example_job:
    name: Generate output
    runs-on: ubuntu-latest
    steps:
      - name: printing
        run: echo " print"
